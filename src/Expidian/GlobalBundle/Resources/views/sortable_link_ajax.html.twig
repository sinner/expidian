<a {% for attr, value in options %} 
     {% if attr!='href' %}
          {{ attr }}="{{ value }}"
     {% else %}
          onClick="ordenar_resultados('{{ value }}');"
     {% endif %}
   {% endfor %}
>
   {{ title }}
</a>